server {
  listen       80;
  #server_name  local.ouhua-group.com;
  server_name   test.com;
  client_max_body_size 2m;
  #charset koi8-r;

  #access_log  logs/host.access.log  main;


  #root 路径配置 在location外面可以下面路径可以得以继承
  #入口地址以及php访问地址
  #root   /var/www/store/Web/;
  #root   /var/www//Laravel5.2/blog/public;
  #root   /var/www/blog;
  #root   /var/www/tp51_swoole/public;
  #root   /var/www/tp51/public;
  #root   /var/www/tp50/public;
  #root   /var/www/shop/web;
  #root   /var/www/WebSocket;
  #root   /var/www/StorageOrder;
  #root   /var/www/xfPay;
  #root   /var/www/redisTest;
  #root   /var/www;
  #root   /var/www/yuyinshibie;
  #root   /var/www/wx/fronted/web;
  #root   /var/www/daojishi;

  #start 公司项目
  # root   /var/www/Code/caesarSystemV2-V5.0_CMS/sadmin;
  #end

  #.htaccess文件地址
  #include /var/www/store/Web/.htaccess;

  #php 解析需要以下加上 index.php
  location / {
  #root   /var/www/;
index  index.html index.htm index.php;
# 重写url
#if (!-e $request_filename) {
#   rewrite  ^(.*)$  /index.php?s=/$1  last;
#}
try_files $uri $uri/ /index.php?s=$uri&$query_string;
}

#error_page  404              /404.html;

# redirect server error pages to the static page /50x.html
#
error_page   500 502 503 504  /50x.html;
location = /50x.html {
root   html;
}

#	location ~ .*\.(gif|jpg|jpeg|png|bmp|swf)$
#	{
#		valid_referers none blocked http://localhost:80901;
#		if ($invalid_referer)
#		{
#			return 403;
#		}
#		expires 30d;
#       }

# proxy the PHP scripts to Apache listening on 127.0.0.1:80
#
#location ~ \.php$ {
#   proxy_pass   http://127.0.0.1;
#}

# pass the PHP scripts to FastCGI server listening on 127.0.0.1:9000
#

#php解析需要配置以下参数
location ~ \.php$ {
#root           /var/www;
fastcgi_pass   127.0.0.1:9000;
fastcgi_index  index.php;
#fastcgi_param  SCRIPT_FILENAME  /scripts$fastcgi_script_name;
fastcgi_param  SCRIPT_FILENAME  $document_root$fastcgi_script_name;
include        fastcgi_params;
}

# deny access to .htaccess files, if Apache's document root
# concurs with nginx's one
#
#location ~ /\.ht {
#    deny  all;
#}
}

